name: app_inscripcionflutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  http: ^1.1.0                
  flutter_spinkit: ^5.2.0  
  provider: ^6.1.1                # Para gestión de estado
  shared_preferences: ^2.2.2      # Para persistencia de datos (localStorage)


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  
